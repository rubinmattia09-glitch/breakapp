# Dipendenze e build
node_modules
dist

# Segreti: MAI caricare le chive API!
.env
.env.openai
.env.*

# Dati interni dell'assistente / log
.workbuddy-ai
*.log
npm-debug.log*

# Database SQLite (generato a runtime; non committare).
# NOTA: /data/ (con lo slash iniziale) riguarda SOLO la cartella alla radice
# (dove vive data/app.db). NON deve escludere src/data/ (codice sorgente!).
/data/
*.sqlite
*.sqlite-journal
*.db
